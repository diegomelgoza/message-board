<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <a href="/new">New Message</a>
    <ul>
      <% for( message of messages ) { %>
      <li>
        <div class="card">
          <h3><%= message.text %></h3>
          <p>-<%= message.user %></p>
          <p><%= message.added %></p>
        </div>
      </li>
      <% } %>
    </ul>
  </body>
</html>
